イベント:
  - label: プレイヤーがうごいたら
    code: |
      mc.player.on(('▼ プレイヤーが', 'うごいた'), () => {


        /*+ アクション */

      });
  - label: プレイヤーがやられたら
    code: |
      mc.player.on(('▼ プレイヤーが', 'やられた'), () => {


      	/*+ アクション */

      });
  - label: ブロックがこわれたら
    code: |
      mc.blocks[('▼ ブロック', 'grass')].onこわれた = () => {


        /*+ アクション */

      };
  - label: ブロックがおかれたら
    code: |
      mc.blocks[('▼ ブロック', 'grass')].onおかれた = () => {


        /*+ アクション */

      };
