じどう:
  - label: 自動こうげき
    description: こうげきさせる
    code: |
      await self.attack(); // こうげきする

  - label: 自動あるき
    description: 一歩前にすすませる
    code: |
      await self.walk(); // あるく

  - label: 自動ターン
    description: 時計回りに１回ターンさせる
    code: |
      await self.turn(1); // ターンする

  - label: きゅうけい
    description: １秒だけそこで止める
    code: |
      await self.wait(1); // 少しやすむ

  - label: 自動をやめる
    description: 決められた回数で、自動せいぎょをやめる
    code: |
      // 何回目？
      if (count === 5) {
        self.endless(); // おわり
      }
