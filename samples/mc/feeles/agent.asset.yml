エージェント:
  - label: 移動
    code: |
        await mc.agent.move(('▼ 方向', 'forward'));
  - label: 方向転換
    code: |
        await mc.agent.turn(('▼ 回転方向', 'left'));
  - label: 攻撃
    code: |
        await mc.agent.attack(('▼ 方向', 'forward'));
  - label: こわす
    code: |
        await mc.agent.destroy(('▼ 方向', 'forward'));
  - label: アイテムを落とす
    code: |
        await mc.agent.drop(1, 64, ('▼ 方向', 'forward'));
  - label: 全てのアイテムを落とす
    code: |
        await mc.agent.dropAll(('▼ 方向', 'forward'));
  - label: しらべる
    code: |
        await mc.agent.inspect(('▼ 方向', 'forward'));
  - label: しらべる（データ）
    code: |
        await mc.agent.inspectData(('▼ 方向', 'forward'));
  - label: 検出
    code: |
        await mc.agent.detect(('▼ 方向', 'forward'));
  - label: レッドストーンを検出
    code: |
        await mc.agent.detectRedstone(('▼ 方向', 'forward'));
  - label: スロット移動
    code: |
        await mc.agent.transfer(1, 64, 2);
  - label: エージェントをつくる
    code: |
        await mc.agent.create();
  - label: プレイヤーの位置に移動
    code: |
        await mc.agent.tp();
  - label: アイテムをあつめる
    code: |
        await mc.agent.collect(('▼ アイテム', 'diamond'));
  - label: たがやす
    code: |
        await mc.agent.till(('▼ 方向', 'forward'));
  - label: アイテムをおく
    code: |
        await mc.agent.place(1, ('▼ 方向', 'forward'));
  - label: アイテムの数を取得する
    code: |
        await mc.agent.getItemCount(1);
  - label: アイテムの空きを取得する
    code: |
        await mc.agent.getItemSpace(1);
  - label: アイテムの詳細を取得する
    code: |
        await mc.agent.getItemDetail(1);
